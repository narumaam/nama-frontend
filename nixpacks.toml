[phases.setup]
nixPkgs = ["python310", "gcc"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "python main.py"

[variables]
PYTHONPATH = ".:backend"
